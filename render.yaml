services:
  - type: web
    name: multilingual-transcriber
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: LOG_LEVEL
        value: INFO
      - key: OLLAMA_HOST
        value: http://localhost:11434
